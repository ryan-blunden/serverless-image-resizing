#!/bin/bash

set -o errexit

stack_name=ServerlessImageResize

aws s3 rb s3://${image_bucket_name} --force
aws s3 rb s3://${deployment_bucket_name} --force
aws cloudformation describe-stacks --stack-name ${stack_name}


